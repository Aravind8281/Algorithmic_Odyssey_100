Queue Implementation Java !!!

add(element) - add element to rear of the queue and return error if queue is full
offer(element) - add element to rear of the queue and return false if queue is full
poll() - remove the element in front of queue and return false if queue is empty
remove() - remove the element in front of queue and return error if queue is empty
peek() - return the rear element in the queue
size() - return the number of element in queue
clear() - empty the queue
remove(index) - element in specific index
get(index) - get element in specific index
contains(element) - check whether queue consists element
